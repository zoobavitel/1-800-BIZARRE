# Do not set REACT_APP_API_URL to localhost — that bakes into GitHub Pages and triggers
# “Local Network Access” / blocked requests to the player’s own machine.
# Players set the host’s URL via Server URL on login (stored in localStorage).
# To ship a fixed public API, set REACT_APP_API_URL in CI before `npm run build`.
